apiVersion: v1
kind: Service
metadata:
  name: k8s-mcp-app-service
  namespace: mcp-demo
  labels:
    app: k8s-mcp-app
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: 3000
    protocol: TCP
    name: http
  selector:
    app: k8s-mcp-app

